# customer_support_chatbot
Hybrid AI customer support chatbot combining semantic intent classification (sentence-transformers + logistic regression) with a FLAN-T5 generative fallback. Features context-aware conversations, calibrated confidence scoring, FastAPI backend, and a Streamlit chat UI.
